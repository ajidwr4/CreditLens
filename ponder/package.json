{
  "name": "my-app",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "dotenv -e .env -- ponder dev",
    "start": "dotenv -e .env -- ponder start",
    "db": "dotenv -e .env -- ponder db",
    "codegen": "ponder codegen",
    "serve": "dotenv -e .env -- ponder serve",
    "lint": "eslint .",
    "typecheck": "tsc"
  },
  "dependencies": {
    "drizzle-kit": "0.22.8",
    "hono": "^4.5.0",
    "ponder": "^0.16.3",
    "viem": "^2.21.3"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "dotenv-cli": "^11.0.0",
    "eslint": "^8.54.0",
    "eslint-config-ponder": "^0.16.3",
    "typescript": "^5.3.2"
  },
  "engines": {
    "node": ">=18.14"
  }
}